
//take destructured props named 'title' i parameter
const Title = ({title})=>{
    return(
        /* return title in h1-tag */
    <div>
    <h1>{title}</h1>
    </div>
    )
}

export default Title;